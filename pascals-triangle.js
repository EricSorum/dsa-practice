var generate = function(numRows) {
  /*
  So each number is like
  take the subarray before this subarray
  and take the item in that array that's one behind the current index
  and add it to the item after it

    
  */

  // but we're creating the array as we go.
  let triangle = [];
  // let counter = 1;
  for (let i = 0; i < numRows; i++) {
    
  }
};